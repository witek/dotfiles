;; -*- lexical-binding: t; -*-

;; Author: Witoslaw Koczewski <wi@koczewski.de>

;;; mu4e

(add-to-list 'load-path "/usr/share/emacs/site-lisp/mu4e")

;; (use-package mu4easy
  ;; :config
  ;; (mu4easy-mode)
         ;; )

;;; provide
(provide 'my-email)
